<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Julio Cesar dos Santos Junior - Data Engineer</title>
    <style>
        :root {
            --bg-dark: #12091f;
            --bg-sidebar: #1a0f2e;
            --purple-primary: #7b2cbf;
            --purple-glow: #9d4edd;
            --text-main: #e0aaff;
            --text-sub: #c77dff;
            --white: #ffffff;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background-color: var(--bg-dark);
            color: var(--white);
            display: flex;
            height: 100vh;
            overflow: hidden;
        }

        /* Sidebar */
        .sidebar {
            width: 260px;
            background-color: var(--bg-sidebar);
            border-right: 1px solid rgba(157, 78, 221, 0.2);
            display: flex;
            flex-direction: column;
            padding: 30px 20px;
            z-index: 10;
        }

        .sidebar h2 {
            font-size: 1.1rem;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            color: var(--text-sub);
            margin-bottom: 25px;
            border-bottom: 1px solid rgba(157, 78, 221, 0.2);
            padding-bottom: 10px;
        }

        .sidebar ul {
            list-style: none;
        }

        .sidebar li {
            margin-bottom: 15px;
        }

        .sidebar a {
            color: var(--white);
            text-decoration: none;
            font-size: 1rem;
            display: block;
            padding: 10px 15px;
            border-radius: 8px;
            transition: all 0.3s ease;
            background: rgba(123, 44, 191, 0.1);
        }

        .sidebar a:hover {
            background: var(--purple-primary);
            color: var(--white);
            transform: translateX(5px);
            box-shadow: 0 0 15px rgba(157, 78, 221, 0.4);
        }

        /* Main Content */
        .main-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 40px;
            position: relative;
        }

        .hero h1 {
            font-size: 3.5rem;
            font-weight: 700;
            margin-bottom: 10px;
            background: linear-gradient(45deg, #e0aaff, #9d4edd, #c77dff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-shadow: 0 0 30px rgba(157, 78, 221, 0.3);
        }

        .hero h2 {
            font-size: 1.8rem;
            color: var(--text-sub);
            font-weight: 400;
            letter-spacing: 2px;
        }

        /* Gengar no canto inferior esquerdo */
        .gengar-container {
            position: fixed;
            bottom: 20px;
            left: 20px;
            z-index: 100;
            pointer-events: none;
        }

        .gengar-container img {
            width: 140px;
            height: auto;
            filter: drop-shadow(0 0 12px rgba(157, 78, 221, 0.7));
            animation: float 4s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-8px); }
        }

        /* Responsividade para telas menores */
        @media (max-width: 768px) {
            body {
                flex-direction: column;
                overflow: auto;
            }
            .sidebar {
                width: 100%;
                height: auto;
            }
            .hero h1 {
                font-size: 2.2rem;
            }
            .hero h2 {
                font-size: 1.3rem;
            }
        }
    </style>
</head>
<body>

    <!-- Aba Lateral -->
    <aside class="sidebar">
        <h2>Navegação</h2>
        <ul>
            <li><a href="#" target="_blank">Sobre Mim</a></li>
            <li><a href="#" target="_blank">Projetos</a></li>
            <li><a href="#" target="_blank">GitHub</a></li>
            <li><a href="#" target="_blank">LinkedIn</a></li>
            <li><a href="#" target="_blank">Contato</a></li>
        </ul>
    </aside>

    <!-- Conteúdo Principal -->
    <main class="main-content">
        <div class="hero">
            <h1>Julio Cesar dos Santos Junior</h1>
            <h2>Data Engineer</h2>
        </div>
    </main>

    <!-- Gengar no Canto Inferior Esquerdo -->
    <div class="gengar-container">
        <img src="https://encrypted-tbn0.gstatic.com/licensed-image?q=tbn:ANd9GcTe3vZB2s8JEa0bAJKk74ulTV3zhJmbJM2XDd6EQ1UaWERjK9i9J3hCRw-deMpB14wlZYecT6y-C6RjVLY" alt="Gengar" />
    </div>

</body>
</html>